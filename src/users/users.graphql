type User {
  id: ID!
  email: String
  phoneNumber: String
}

type Query {
  user(id: ID!): User
}

type Mutation {
  createUser(email: String, phoneNumber: String): User!
  updateUser(id: ID!, email: String, phoneNumber: String): User!
  deleteUser(id: ID!): Boolean!
}
