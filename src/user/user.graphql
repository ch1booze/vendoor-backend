type User {
  id: ID!
  firstName: String
  lastName: String
  business: Business
}

type Query {
  getUser(id: ID!): User
}

type Mutation {
  signinupUser(email: String, phoneNumber: String): Boolean!
  verifyUser(
    preAuthSessionId: String!
    deviceId: String!
    userInputCode: String!
  ): User!
  upsertUserProfile(id: ID!, firstName: String, lastName: String): User!
}
