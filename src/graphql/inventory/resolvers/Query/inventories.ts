import type { QueryResolvers } from './../../../types.generated';
export const inventories: NonNullable<QueryResolvers['inventories']> = async (
	_parent,
	_arg,
	_ctx,
) => {
	/* Implement Query.inventories resolver logic here */
};
