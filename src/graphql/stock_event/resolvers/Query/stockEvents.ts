import type { QueryResolvers } from './../../../types.generated';
export const stockEvents: NonNullable<QueryResolvers['stockEvents']> = async (
	_parent,
	_arg,
	_ctx,
) => {
	/* Implement Query.stockEvents resolver logic here */
};
