scalar JSON

type Business {
  id: ID!
  name: String!
  type: String!
  description: String
  workingHours: JSON
  user: User!
}

type Query {
  getBusiness(id: ID!): Business!
}

type Mutation {
  registerBusiness(
    userId: ID!
    name: String!
    type: String!
    description: String
    workingHours: JSON
  ): Business!
  updateBusiness(
    userId: ID!
    name: String
    type: String
    description: String
    workingHours: JSON
  ): Business!
}
